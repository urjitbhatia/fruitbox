services:
  app:
    image: myapp:latest
    secrets:
      - db_password
      - source: db_password
        target: /custom/secret/path
    configs:
      - app_config

secrets:
  db_password:
    file: ./db_password.txt

configs:
  app_config:
    file: ./app_config.yml
